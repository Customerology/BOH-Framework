# Customerology_Framework

#### Prepare production build
- run `npm run build`

#### Running build locally
- after application build was generated run `npm run lite-server`
- open http://localhost:3000/
